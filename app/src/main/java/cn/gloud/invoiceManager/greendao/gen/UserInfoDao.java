package cn.gloud.invoiceManager.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import cn.gloud.invoiceManager.Entity.UserInfo;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "USER_INFO".
*/
public class UserInfoDao extends AbstractDao<UserInfo, Long> {

    public static final String TABLENAME = "USER_INFO";

    /**
     * Properties of entity UserInfo.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Currentid = new Property(0, long.class, "currentid", true, "_id");
        public final static Property Account_id = new Property(1, int.class, "account_id", false, "ACCOUNT_ID");
        public final static Property Account_name = new Property(2, String.class, "account_name", false, "ACCOUNT_NAME");
        public final static Property Logintoken = new Property(3, String.class, "logintoken", false, "LOGINTOKEN");
        public final static Property Is_admin = new Property(4, int.class, "is_admin", false, "IS_ADMIN");
    }


    public UserInfoDao(DaoConfig config) {
        super(config);
    }
    
    public UserInfoDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"USER_INFO\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL ," + // 0: currentid
                "\"ACCOUNT_ID\" INTEGER NOT NULL ," + // 1: account_id
                "\"ACCOUNT_NAME\" TEXT," + // 2: account_name
                "\"LOGINTOKEN\" TEXT," + // 3: logintoken
                "\"IS_ADMIN\" INTEGER NOT NULL );"); // 4: is_admin
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"USER_INFO\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, UserInfo entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getCurrentid());
        stmt.bindLong(2, entity.getAccount_id());
 
        String account_name = entity.getAccount_name();
        if (account_name != null) {
            stmt.bindString(3, account_name);
        }
 
        String logintoken = entity.getLogintoken();
        if (logintoken != null) {
            stmt.bindString(4, logintoken);
        }
        stmt.bindLong(5, entity.getIs_admin());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, UserInfo entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getCurrentid());
        stmt.bindLong(2, entity.getAccount_id());
 
        String account_name = entity.getAccount_name();
        if (account_name != null) {
            stmt.bindString(3, account_name);
        }
 
        String logintoken = entity.getLogintoken();
        if (logintoken != null) {
            stmt.bindString(4, logintoken);
        }
        stmt.bindLong(5, entity.getIs_admin());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    @Override
    public UserInfo readEntity(Cursor cursor, int offset) {
        UserInfo entity = new UserInfo( //
            cursor.getLong(offset + 0), // currentid
            cursor.getInt(offset + 1), // account_id
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // account_name
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // logintoken
            cursor.getInt(offset + 4) // is_admin
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, UserInfo entity, int offset) {
        entity.setCurrentid(cursor.getLong(offset + 0));
        entity.setAccount_id(cursor.getInt(offset + 1));
        entity.setAccount_name(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setLogintoken(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setIs_admin(cursor.getInt(offset + 4));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(UserInfo entity, long rowId) {
        entity.setCurrentid(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(UserInfo entity) {
        if(entity != null) {
            return entity.getCurrentid();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(UserInfo entity) {
        throw new UnsupportedOperationException("Unsupported for entities with a non-null key");
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
